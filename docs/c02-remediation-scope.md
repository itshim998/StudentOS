# C-02 embedding-space integrity

This branch makes embedding identity explicit, prevents cross-model cosine comparisons, keeps real-provider failures retryable, and introduces compatible pgvector/full-text reciprocal-rank fusion retrieval. The temporary integration scripts and workflows are removed after validation.
